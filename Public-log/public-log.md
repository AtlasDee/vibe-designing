# 公开更新日志 Public Log

> vibe designing skill 的版本与能力演进（对外脱敏版）。最新在上。
> 说明：示例验证涉及的真实业务内容、文档名、链接、系统/项目名称均已移除，仅保留技能本身的能力变化。

## 2026-07-03 (21) — 新增姊妹分件 vibe-designing-publish（成文 PRD → 发布到云文档）

- 新增第 2 个姊妹分件 **vibe-designing-publish**：把成文 PRD 全量发布到云文档协作平台（如 Lark/飞书 docx），并对齐团队需求文档模板的呈现样式（高亮摘要块、多列版本信息卡、标题自动编号、引用块、图片原生描述、宽表格）。
- 前置铁律：**发布前必须先备份**、**发布范围需用户确认**、本地日志与云文档自身日志职责分离、发布用「脚本化 + 全量重发」而非手工逐块改——这是套件里**唯一会写外部系统**的分件，风险控制优先于其余纯文本产出分件。
- 内容涵盖：认证与客户端、目标平台文档块模型速查、常见 API 机制与踩坑（批量增删、表格两阶段建表与列数上限突破、图片上传三步流程、嵌套容器一次建成、限流重试、进度校验）、样式映射方法、md→块解析器设计要点、名词解释精简原则、交付自检清单。
- 附一份通用化的发布脚本骨架作为参考（不含任何真实凭证或文档标识）。
- 套件现为 1 入口 + 5 PRD 分件 + 2 姊妹分件（原型、发布）。

## 2026-07-02 (20) — uiux 全屏截图改用 4K 分辨率，修正原型变形问题

- 根因：全屏截图窗口过窄会命中原型自身的响应式断点，导致桌面布局被压缩折叠成窄屏样式（"变形"），与清晰度倍数（DPR）无关。
- 修复：主功能模块的全屏图（交互概览取材）现要求 `--window-size=3840,2160`（4K）截取，不额外叠加 DPR 倍数；子功能模块的局部图不受影响，维持原有窗口尺寸+2×高清+自动裁白。内联组件的局部图统一从同一张 4K 全屏图裁剪得到。

## 2026-07-02 (19) — 修正文件清单表的一处内部标注错误

- 自查发现项目文件清单里几行的跟踪状态标注与忽略规则不一致，已修正对齐。

## 2026-07-02 (18) — 项目管理规范：每次更新同步记录两处日志

- 明确规则：项目中任意更新（无论是否提交/推送）都要**同轮**记录进内部日志与本公开日志（脱敏后）两处，不再等到推送时才补公开日志。
- push 时的职责简化为核对本次 commit 是否都已有对应公开日志条目，而非重新整理内容。

## 2026-07-02 (17) — 项目管理规范建立

- 确立五条项目管理规范：变更必须双处记录（内部+对外脱敏）、文件清单需含用途与模块归属、提交版本时自动整理版本说明、推送时自动整理版本说明、记录所有分支与 worktree 概述。
- 项目概况文档新增结构总览（说明工作区可容纳多个项目）与完整文件清单。

## 2026-07-02 (16) — prototype 补两条硬规定：命名标注同步 FILE-MAP / 禁止大文件

- **硬规定1**：每个页面/模块文件顶部必须加注释标注「页面（大功能）/ 功能模块（子功能模块-功能名称）」，命名与 [vibe-designing-feature] 的标题命名规范对齐；FILE-MAP.md 必须有一张「文件↔PRD层级」映射表，且随当轮改动同步更新（不拖到下一轮）。
- **硬规定2**：禁止把原型写成一个大文件；按功能类型拆分（框架壳层/数据层/每个组件一文件/公共交互脚本/样式），给出判据与最小拆分单位。
- 自检清单、五阶段总览、输入输出契约同步补充。

---

## 2026-07-02 (15) — 新增姊妹分件 vibe-designing-prototype

- 新增 **vibe-designing-prototype**（需求 → 可交互 HTML 原型，产原型非 PRD）：五阶段工作流（多源提取→分层归属→控件映射→增量实现→DOM 断言验证+沉淀）；机制级 vs 配置项级判断；**控件模式库**与默认交互范式（凡列表皆可拖拽、结果当前视图可见、单一控件承载多能力…）；工程防坑清单（破缓存/iframe 高度/DnD/parse check…）；指令理解层（八种句式、编号条目=验收清单、新指令覆盖旧指令、方向分叉才提问）。
- uiux 小幅增强：来源表补 figma 帧/图片 PDF 切片的获取方式（仅当被指定为来源）；「交互动词是硬需求」写入交互说明要点。
- 总入口：路由表与管线加入 prototype 姊妹分件说明。套件 = 1 入口 + 5 PRD 分件 + 1 原型分件。

---

## 2026-07-01 (14) — 全套审查：脱敏 + 一致性修复

- **脱敏**：六个 skill 包内的示例全部通用化——移除内部文档名/系统名/UAT 链接/本地路径/真实业务字段（含 field 的 references 范例文件整体重写为通用示例）。
- **一致性**：
  - 统一大功能内部编号：`7.X.1 需求概述 → 7.X.2 交互概览（必有）→ 7.X.3 业务流程图（复杂才有，无则顺延）→ 功能模块`（feature 与 uiux 对齐，总入口示例同步）。
  - 总入口更新为五个子技能（描述/管线/路由/全局约定）。
  - feature：实例按新命名与编号重写；修复输出格式要求重复编号。
  - field：修正引用已不存在「值域」列的两处表述；「模式」术语统一为「情形」（describe/businessflow/field/feature）。
  - uiux：示例命令对齐 2×+等待加载；交互概览从"复杂才有"统一为"每个大功能必有"（业务流程图仍复杂才有）。

---

## 2026-07-01 (13) — UI 图覆盖标准统一

- **vibe-designing-uiux**：统一每类模块给什么图——
  - 主功能模块（大功能）= 全屏上下文图 + 交互概览故事板（全屏图只此一层出现）；
  - 子功能模块 = 一张「配置项局部图」（裁白、2× 高清）；iframe 组件截独立页天然局部，**内联组件裁剪到工作台里该组件的配置项/运营操作区**（不贴整张全屏）；复杂/多态按状态多裁几张。
  - 消除「有的只全屏、有的只局部、有的没图」的混用；补充内联组件按区域 `crop` 的截图技巧与自检项。

---

## 2026-07-01 (12) — 标题命名规范统一

- **vibe-designing-feature**：固化功能详细说明的标题命名（house style）——
  - 大功能 = `主功能模块名称-系统名称`（多系统时带系统名，如 `页面装修-运营中心`）；
  - 功能模块 = `功能模块：子功能模块名称-功能名称`（如 `功能模块：爆奖列表-样式配置`）。
  - 全篇统一，禁止「工作台/中心/编辑器/配置」等随意后缀混用。自检项同步增补。

---

## 2026-07-01 (11) — 三条硬规定：模块不合并 / 复杂多图 / 字段列不精简

针对实跑 PRD 暴露的三问题，强化三个子技能：

- **feature（模块粒度）**：一个组件/独立配置面板/独立抽屉 = 一个独立功能模块；⛔ 禁止用 `(1)(2)…` 把多个组件揉进一个模块、禁止合并两个不同组件。
- **uiux（图的覆盖）**：每个功能模块必须至少 1 张 UI 图；**复杂/多态模块必须按状态多截图**（条件显隐/弹窗抽屉/分支进度各态各一张，从"可多张"改为硬要求）；每个复杂大功能必须有交互概览故事板。
- **field（字段列）**：必须用完整列模板（搜索/编辑 11 列、列表 6 列）；⛔ 禁止自造精简列子集/删列；空值填 `-`；数据来源/提示文案/反馈/示例/报错文案不得省略。
- 三技能自检清单同步增补。

---

## 2026-07-01 (10) — uiux 强制内嵌：禁止独立交互章节

- 硬规定：⛔ 不得另起「交互原型/交互说明」独立章节或附录；UI 图 + 交互说明必须就地内嵌到每个功能模块，交互概览作大功能下的 `7.X.2`。
- ❌ 禁止用「(对应 7.x)」交叉引用代替内嵌；注意生成顺序——写完功能详细说明后要回到各模块就地插入，而非新开一章追加。
- 判据：删掉任何交互章节后，各功能模块自身仍应看得到 UI 图与交互说明。自检清单同步增补。

---

## 2026-06-30 (9) — 交互概览改为画板/故事板

- **交互概览硬要求**：把页面截图用箭头 + 步骤说明横向串联成故事板，不再只写 `①→②→③` 文字主线（文字仅作图注）。
- 生成方式：用 PIL 拼接截图（统一高度横排 + 箭头 + 步骤徽标 + 说明）导出一张概览图。
- 自检项同步：交互概览须为画板/故事板。

## 2026-06-30 (8) — uiux 截图质量：高清 + 自动裁白 + 图标加载

- **高分辨率**：截图加 `--force-device-scale-factor=2`（2× 高清）。
- **图标/字体加载**：加 `--virtual-time-budget`，等图标字体加载完再截，避免占位框。
- **自动裁白**：截后用 PIL `ImageChops.difference + getbbox` 裁掉四周纯色边，小卡片页只留元素。

## 2026-06-30 (7) — vibe-designing-uiux 经验固化

- **来源铁律**：UI 图只用所提供的交互原型截图，同项目来源统一；未指定来源一律不用。
- **截图方式**：首选无头浏览器 `--screenshot` 直接落盘；交互态用「注入自动展开脚本 + virtual-time-budget」的临时副本页，截完即删。浏览器扩展类截图可能不回传可用磁盘路径，不据此嵌入文档。
- **双重复查**：截图后逐张复查（真图/尺寸/内容正确）；嵌入后复查（路径/显示/图注相符）。

## 2026-06-30 (6) — 新增子技能 vibe-designing-uiux（原型图与交互说明）

- 新增第 6 个 skill：依据功能清单为每个功能模块找交互原型，把 UI 页面图 + 交互概览 + 交互说明嵌入「功能详细说明」；复杂功能点补核心业务流程交互图。
- 放置层级：大功能层「交互概览 / 业务流程图」；功能模块层在字段表/功能说明表前贴 UI 图 + 图注 + 交互说明。
- 总入口 pipeline 增「What·界面」，套件由 5 件变 6 件。

## 2026-06-30 (5) — 规范：阿拉伯数字序号 + 三类字段表

- 全局约定：章节序号统一阿拉伯数字（`1` / `1.1` / `7.1.2`），不用中文大写。
- 三类字段表列名统一：搜索字段说明 / 编辑字段说明 = 11 列（字段中/英文·类型·是否必填·交互类型·数据来源·提示文案·默认值·反馈·示例·规则描述）；列表字段说明 = 6 列（字段中/英文·数据类型·数据来源·示例·描述）。

## 2026-06-30 (4) — 结构规范：功能详细说明三级层级

- 固化「功能详细说明」章节结构：大功能 `7.X` → `7.X.1 需求概述` → `7.X.2/.3… 功能模块：xxx`；一个大功能可含多个功能模块，每个功能模块内放 字段说明表 + 状态-操作矩阵 + 功能说明表。

## 2026-06-30 (3) — 方法升级：四种情形 + 输入优先级

- 「三种工作模式」升级为**四种情形**（按现有 PRD 完善度）：①无 PRD→从零生成 ②有但不完善→补全 ③仅细微不规范→检查修正 ④已完善→不调整。
- 新增**输入来源优先级（不只依赖原始需求）**：现有 PRD（基准）＞ 交互原型/项目资料（落地，常修正原始需求）＞ 原始需求（方向）。
- 四种情形/输入原则贯穿到 功能清单/功能模块/需求概述/字段说明/功能说明 各产出。

## 2026-06-30 (2) — vibe designing skill 立项与构建

- 确立目标：产品文档整理技能（业务需求 → 产品方案 → 结构规范的 PRD）。
- 构建「1 入口 + 4 分件」共 5 个 skill：
  - 总入口/编排
  - describe（摘要/版本/变更日志/背景期望）
  - businessflow（方案目标/名词/流程/架构/功能清单/Out-of-Scope）
  - field（搜索/编辑/列表三类字段表，含新增的编辑配置字段表）
  - feature（需求概述/功能说明表/状态矩阵/权限/日志）
- 设计依据：归纳 PRD 写作 house style + 需求文档模板，吸收通用 PM 方法论（摘要句式、成功指标三件套、显式 Out-of-Scope、🔶🔵 行内缺口标记、产出自检）。
- 旧的两个 skill 被升级取代；各 skill 以 `.skill`（ZIP，内含 `SKILL.md`）打包。

## 2026-06-30 (1) — 初始化

- 建立项目与版本日志。
- 沉淀两个初始 skill（字段说明格式化、功能说明梳理），后续升级为 field / feature 分件。
